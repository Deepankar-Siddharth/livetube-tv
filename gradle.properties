org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# These values are intentionally empty in source control. Configure them in CI or with
# -Pgithub.owner=... -Pgithub.repository=... when building an installable configuration.
github.owner=
github.repository=
github.branch=main

# LiveTube TV and NewPipeExtractor intentionally have independent versions.
app.version=1.0.1
newpipe.version=v0.26.5
